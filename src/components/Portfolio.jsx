import styles from "./Portfolio.module.scss";

const Portfolio = () => {
  return (
    <section className={styles.portfolio}>
      <h2>Projects</h2>
    </section>
  );
};

export default Portfolio;
